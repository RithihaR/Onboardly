// app/api/questions/route.ts
// Placeholder — replace with real logic. This exists just to satisfy
// Next.js/TypeScript's requirement that every route.ts export a handler.
import { NextResponse } from 'next/server';
 
export async function GET() {
  return NextResponse.json({ message: 'questions route not yet implemented' });
}
 